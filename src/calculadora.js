export function calcularArea(comprimento, largura) {
    const comprimentoEmMetros = comprimento / 100;
    const larguraEmMetros = largura / 100;
    return comprimentoEmMetros * larguraEmMetros;
  }
  